import React from "react";
import "./spinner.css";

const Spinner = () => {
  return (
    <div className="spinner">
      <span className="loader"></span>
    </div>
  );
};

export default Spinner;
